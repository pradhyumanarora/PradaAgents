# Backend dependencies (extends the project requirements.txt)

# Core AutoGen packages
autogen-agentchat>=0.4.0
autogen-core>=0.4.0
autogen-ext[openai,azure]>=0.4.0

# OpenAI client
openai>=1.0.0

# Async support
aiohttp>=3.8.0

# YAML config parsing
pyyaml>=6.0

# Pydantic for data validation
pydantic>=2.0.0

# Environment variable management
python-dotenv>=1.0.0

# FastAPI backend
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# Database (PostgreSQL)
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
